# 2026-05 Interaction Log

## 2026-05-07

- Discussed the project north star: AI is too fire-and-forget; it needs a
  debuggable pipeline.
- Captured elevator phrase: local enough to compose, remote enough to automate,
  simple enough to inspect, neutral enough for communities.
- Mapped major roots: Unix, Bash, Azure CLI, C# reflection/attributes, C#
  partial/interface ideas, STL, XSD, Trello, npm.
- Reviewed `cursor-*` decomposition as STL-inspired cursors, ranges, algorithms,
  containers, views, and adapters.
- Reviewed `partial-*` decomposition as descriptor copying, partial classes,
  concepts, shapes, metadata, and precondition thunking.
- Added `@kingjs/function-contract` and `@kingjs/cursor-checks` as initial
  standalone function contract/check packages.
- Discussed future XSD-style `[Restrictions]` for named check derivation.
- Reviewed `cli-*` packages as Azure CLI/Bash/Unix-inspired command hierarchy,
  runtime, parser, output, and shell interop work.
- Added public-facing docs under `docs/`.
- Added assistant-facing memory under `gpt/`.
- Refined `gpt/roots/` into one directory per root with one file per translated
  concept.
- Updated C# attributes root note to discuss valid metadata value types and
  symbol-attached metadata blocks.
- Added this monthly interaction log.

## 2026-05-07 Later

- Added policy that root concept files should be named after source-system
  features so familiar roots are visible in directory listings.
- Renamed root notes toward source feature names, such as
  `csharp/custom-attributes.md` and `stl/algorithms.md`.
- Added `stl/requires-expressions.md` to capture the mapping from C++20
  anonymous `requires` expressions and named concepts to function-contract
  procedural checks and named `Check` types.
- Added policy that assistants may proactively update `gpt/` memory when
  durable preferences or mappings emerge, while keeping source edits
  instruction-driven.
- Refined `stl/requires-expressions.md` to show the newer
  `contract(metadata, function)` style, where preconditions/defaults appear in
  a symbol-keyed metadata block before the function body, closer to C# custom
  attributes.
- Added `gpt/partial/` as a recursive assistant-memory tree for the `partial-*`
  packages, including a TOC, recursive documentation policy, package map,
  package summaries, and deeper notes for concepts like meta-prototype chains,
  transparent partials, associated types, conditions, thunks, and
  shape-vs-concept.
- Added `gpt/marketing/` for pitch/explanation patterns, starting with an
  "idioms made declarative" example showing how a raw JavaScript
  `Object.defineProperty` const-field idiom maps to declarative `Defines`
  metadata.
- Moved marketing hooks to the top of idiom notes and added more examples:
  abstract methods, interface contracts, mixins, method preconditions, default
  helper methods, and runtime duck checks.
- Refined marketing idiom format: start with a tiny pitch line, then the hook
  text, without a `Hook` subheader. Grouped idioms in the TOC by descriptor,
  contract, composition, and runtime-shape idioms.
- Added marketing example policy: when prose claims that a concept, part, or
  metadata declaration can be consumed by concrete types, continue through a
  small concrete implementation and call-site example.
- Added the idea of test harvesting as a GPT compile step: scan focused tests
  for small proof stories and turn them into marketing/documentation example
  candidates.
- Expanded the marketing future-example backlog across metadata behavior,
  contracts, cursors, CLI/shell, Unix streams, and reflection.
- Drafted a first batch of marketing idiom notes from the backlog and tests,
  emphasizing nagging JavaScript pain points followed by declarative
  translations and concrete payoffs.
- Added marketing guidance to name a feature's lineage when the analogy to an
  existing technology is tight enough to create familiarity, such as STL
  associated types, C# attributes, Bash `read`, Unix pipelines, or Azure CLI
  help.
- Refined marketing lineage style: put lineage near the end under its own
  header, after the local idiom and declarative upgrade, and include a small
  source-to-translation example when useful.
