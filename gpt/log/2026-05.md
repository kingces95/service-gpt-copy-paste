# 2026-05 Interaction Log

## 2026-05-07

- Discussed the project north star: AI is too fire-and-forget; it needs a
  debuggable pipeline.
- Captured elevator phrase: local enough to compose, remote enough to automate,
  simple enough to inspect, neutral enough for communities.
- Mapped major roots: Unix, Bash, Azure CLI, C# reflection/attributes, C#
  partial/interface ideas, STL, XSD, Trello, npm.
- Reviewed `cursor-*` decomposition as STL-inspired cursors, ranges, algorithms,
  containers, views, and adapters.
- Reviewed `partial-*` decomposition as descriptor copying, partial classes,
  concepts, shapes, metadata, and precondition thunking.
- Added `@kingjs/function-contract` and `@kingjs/cursor-checks` as initial
  standalone function contract/check packages.
- Discussed future XSD-style `[Restrictions]` for named check derivation.
- Reviewed `cli-*` packages as Azure CLI/Bash/Unix-inspired command hierarchy,
  runtime, parser, output, and shell interop work.
- Added public-facing docs under `docs/`.
- Added assistant-facing memory under `gpt/`.
- Refined `gpt/roots/` into one directory per root with one file per translated
  concept.
- Updated C# attributes root note to discuss valid metadata value types and
  symbol-attached metadata blocks.
- Added this monthly interaction log.

## 2026-05-07 Later

- Added policy that root concept files should be named after source-system
  features so familiar roots are visible in directory listings.
- Renamed root notes toward source feature names, such as
  `csharp/custom-attributes.md` and `stl/algorithms.md`.
- Added `stl/requires-expressions.md` to capture the mapping from C++20
  anonymous `requires` expressions and named concepts to function-contract
  procedural checks and named `Check` types.
- Added policy that assistants may proactively update `gpt/` memory when
  durable preferences or mappings emerge, while keeping source edits
  instruction-driven.
- Refined `stl/requires-expressions.md` to show the newer
  `contract(metadata, function)` style, where preconditions/defaults appear in
  a symbol-keyed metadata block before the function body, closer to C# custom
  attributes.
- Added `gpt/partial/` as a recursive assistant-memory tree for the `partial-*`
  packages, including a TOC, recursive documentation policy, package map,
  package summaries, and deeper notes for concepts like meta-prototype chains,
  transparent partials, associated types, conditions, thunks, and
  shape-vs-concept.
- Added `gpt/marketing/` for pitch/explanation patterns, starting with an
  "idioms made declarative" example showing how a raw JavaScript
  `Object.defineProperty` const-field idiom maps to declarative `Defines`
  metadata.
- Moved marketing hooks to the top of idiom notes and added more examples:
  abstract methods, interface contracts, mixins, method preconditions, default
  helper methods, and runtime duck checks.
- Refined marketing idiom format: start with a tiny pitch line, then the hook
  text, without a `Hook` subheader. Grouped idioms in the TOC by descriptor,
  contract, composition, and runtime-shape idioms.
- Added marketing example policy: when prose claims that a concept, part, or
  metadata declaration can be consumed by concrete types, continue through a
  small concrete implementation and call-site example.
- Added the idea of test harvesting as a GPT compile step: scan focused tests
  for small proof stories and turn them into marketing/documentation example
  candidates.
- Expanded the marketing future-example backlog across metadata behavior,
  contracts, cursors, CLI/shell, Unix streams, and reflection.
- Drafted a first batch of marketing idiom notes from the backlog and tests,
  emphasizing nagging JavaScript pain points followed by declarative
  translations and concrete payoffs.
- Added marketing guidance to name a feature's lineage when the analogy to an
  existing technology is tight enough to create familiarity, such as STL
  associated types, C# attributes, Bash `read`, Unix pipelines, or Azure CLI
  help.
- Refined marketing lineage style: put lineage near the end under its own
  header, after the local idiom and declarative upgrade, and include a small
  source-to-translation example when useful.
- Started `gpt/meta-prototype-chain/` to document the design spine: prototype
  chain transforms, reflection over transformed chains, and the meaning each
  transform describes. Cataloged ES6 static reflection, partial linearization,
  compiled descriptors, transparent attachments, partial metadata, condition
  chains, reflection info, and shape observation.
- Refined the meta-prototype-chain notes to present transforms as a tree:
  `Es6Reflector` as the base reflection trunk, `PartialReflect` as the main
  composition branch, `PartialMetadata` as a transform over that branch, and
  condition chains as a further metadata expansion.
- Updated meta-prototype-chain diagrams to use the repo's source-comment tree
  style with `├─`, `└─`, and `│` edges.
- Added a meta-prototype-chain example format: show source declarations, the
  source/raw chain, the transformed chain or projection, and the query or
  behavior unlocked by the transform. Retrofitted the transform notes with
  source/before/after examples.
- Expanded the meta-prototype-chain README with a provenance story: ordinary
  instance prototype chains, static prototype chains, `Es6Reflector` static
  reflection transforms, `PartialReflect` composition transforms,
  `PartialMetadata`, and condition-chain derivations form a familiar lineage
  rather than a new idea from nowhere.
- Promoted the meta-prototype-chain doctrine near the top of the README:
  preserve source distinctions that matter, remove artifacts that do not, and
  transform the chain so the right question becomes a normal reflection query.
- Captured a test-design preference: use root POJO matrices for test
  dimensions, make each case an `it` clause, and use runtime `instanceof`
  concept checks to select which types support each case. Treat this as a
  first-touch design feedback loop and a documentation/marketing harvest
  source.
- Added STL template provenance notes: use `generic(..., T => contract(...))`
  to separate template/type metadata from runtime argument metadata, support
  `.as(Type)` explicit instantiation, and let `Shape` mean constructed-instance
  shape in template slots while retaining instance shape in argument slots.
- Refined the Shape vocabulary: rename the current observational wild-value
  `Shape` role toward `Probe`, then reclaim `Shape` for anonymous
  concept/requires-expression-style structural requirements. Keep `Concept` as
  certified opt-in vocabulary and avoid magical `new T()` activation in generic
  template checks.
- Added `@kingjs/weak-map-lookup` as the tuple-to-leaf-`WeakMap` primitive and
  `@kingjs/instantiate` as the cached generic metadata application primitive.
  This supports later lowercase factories like `constructs(...)` producing
  inert applied types such as `ConstructsOf`.
- Added `@kingjs/constructs`, with lowercase `constructs(...requirements)`
  producing cached `ConstructsOf` metadata types. `Constructs` owns the
  `instanceof` semantics by checking a candidate constructor's prototype
  against each requirement.
- Added `@kingjs/metadata` as the tiny inert base class and updated
  `Constructs` to extend it, hoisting the "metadata cannot be instantiated"
  behavior out of the constructs-specific package.
- Replaced the short-lived `@kingjs/instantiate` package with
  `@kingjs/templatize`, which attaches `.as(...)` to function/type template
  definitions, caches specializations with `WeakMapLookup`, and records frozen
  `.targs` for reflection. Updated `@kingjs/constructs` to expose templated
  `Constructs.as(...)` instead of lowercase `constructs(...)`.
- Overhauled `@kingjs/function-contract` to remove legacy `ArgCheck` /
  procedural-check branches. The loader now applies defaults and performs only
  `value instanceof type` slot checks. Custom contract behavior moves behind
  `Symbol.hasInstance` on the type itself; `@kingjs/cursor-checks` was updated
  to match.
- Simplified `@kingjs/function-contract` again by dropping defaults. The
  contract layer is now only `contract(types, fn)`, with checked-thunk support
  when `fn` is omitted. Defaults are caller/template concerns rather than
  loader concerns.
- Converted `cursor-algorithm/materialize` to the first concrete
  `@kingjs/templatize` algorithm: `materialize.as(VectorMap)(first, last)`.
  The template slot uses nested requirements
  `[DefaultConstructible, Constructs.as(PushShape)]` so one type argument must
  both construct and produce instances with `push`.
- Made `VectorMap` the exported default specialization of `materialize`, so
  ordinary calls use `materialize(first, last)` while alternate containers still
  use `materialize.as(type)(first, last)`. This confirms the pattern that a
  default is just the specialization chosen for export.
- Moved the `materialize` implementation/default specialization into
  `cursor-container` because the default depends on `VectorMap`. The
  `cursor-algorithm` package now re-exports it for compatibility, avoiding the
  algorithm module owning a default container dependency directly.
